---
title: "CINDI: Conditional Imputation and Noisy Data Integrity with Flows in Power Grid Data"
title_zh: CINDI：基于流模型的电网数据条件插补与噪声数据完整性研究
authors: "David Baumgartner, Helge Langseth, Heri Ramampiaro"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11745v1"
tags: ["query:hp-pg"]
score: 8.0
evidence: 电力电网中的异常检测与数据完整性
tldr: 针对电力网等关键基础设施中多元时间序列数据的噪声和异常问题，本文提出了一种名为CINDI的无监督概率框架。该框架利用条件归一化流将异常检测与缺失值填补统一在端到端系统中。通过建模数据的精确条件似然，CINDI能识别低概率片段并迭代采样统计一致的替代值，在保持物理和统计特性的同时，显著提升了数据完整性维护的效率和可靠性。
selection_source: fresh_fetch
motivation: 传统的数据清洗方法通常将异常检测与填补分离，难以捕捉数据的完整联合分布并忽略了预测的不确定性。
method: 提出基于条件归一化流的端到端框架，通过建模条件似然来识别异常并迭代采样生成统计一致的修复数据。
result: 在挪威配电网真实损耗数据上的实验表明，CINDI在处理噪声环境下的数据完整性方面优于现有竞争基准。
conclusion: CINDI为复杂多元时间序列提供了一种可扩展且稳健的解决方案，能够有效维护关键基础设施系统的数据可靠性。
---

## 摘要
现实世界中的多元时间序列，特别是在电网等关键基础设施中，经常受到噪声和异常值的干扰，从而降低了下游任务的性能。标准的数据清洗方法通常依赖于不相交的策略，即使用一个模型检测错误，再用另一个模型进行插补。这种方法可能无法捕捉数据的完整联合分布，并忽略了预测的不确定性。本研究提出了“条件插补与噪声数据完整性”（CINDI），这是一个旨在恢复复杂时间序列数据完整性的无监督概率框架。与碎片化的方法不同，CINDI 将异常检测和插补统一到一个基于条件归一化流构建的端到端系统中。通过对数据的精确条件似然进行建模，该框架能够识别低概率片段，并迭代地采样统计一致的替代值。这使得 CINDI 能够高效地重用已学习的信息，同时保留系统底层的物理和统计特性。我们使用来自挪威配电运营商的真实电网损耗数据对该框架进行了评估，尽管该方法旨在推广到任何多元时间序列领域。结果表明，与具有竞争力的基准模型相比，CINDI 表现出稳健的性能，为在噪声环境中维持可靠性提供了一种可扩展的解决方案。

## Abstract
Real-world multivariate time series, particularly in critical infrastructure such as electrical power grids, are often corrupted by noise and anomalies that degrade the performance of downstream tasks. Standard data cleaning approaches often rely on disjoint strategies, which involve detecting errors with one model and imputing them with another. Such approaches can fail to capture the full joint distribution of the data and ignore prediction uncertainty. This work introduces Conditional Imputation and Noisy Data Integrity (CINDI), an unsupervised probabilistic framework designed to restore data integrity in complex time series. Unlike fragmented approaches, CINDI unifies anomaly detection and imputation into a single end-to-end system built on conditional normalizing flows. By modeling the exact conditional likelihood of the data, the framework identifies low-probability segments and iteratively samples statistically consistent replacements. This allows CINDI to efficiently reuse learned information while preserving the underlying physical and statistical properties of the system. We evaluate the framework using real-world grid loss data from a Norwegian power distribution operator, though the methodology is designed to generalize to any multivariate time series domain. The results demonstrate that CINDI yields robust performance compared to competitive baselines, offering a scalable solution for maintaining reliability in noisy environments.

---

## 论文详细总结（自动生成）

以下是对论文《CINDI: Conditional Imputation and Noisy Data Integrity with Flows in Power Grid Data》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在电网等关键基础设施中，多元时间序列数据（如电网损耗）常因传感器故障、传输错误或人为因素产生噪声和异常。
*   **研究动机**：
    *   **现有方法碎片化**：传统做法通常将“异常检测”与“缺失值填补（插补）”视为两个独立任务，使用不同的模型处理，这导致无法捕捉数据的完整联合分布，且忽略了预测的不确定性。
    *   **物理一致性需求**：简单的插值法（如线性插值）会破坏电力系统底层的物理和统计特性，影响下游任务（如日前电网损耗预测）的准确性。
    *   **数据质量瓶颈**：预测模型的性能严格受限于训练数据的质量，因此需要一种能够统一检测、修正并保持数据完整性的框架。

### 2. 核心方法论：CINDI 框架
CINDI（Conditional Imputation and Noisy Data Integrity）是一个基于**条件归一化流（Conditional Normalizing Flows, cNF）**的无监督概率框架。
*   **核心思想**：利用归一化流的可逆性与精确似然估计能力，将异常检测视为“低概率识别”，将插补视为“高概率采样”。
*   **关键技术细节**：
    *   **条件归一化流**：基于 RealNVP 架构，通过学习一系列可逆变换，将复杂的数据分布映射到简单的标准正态分布。模型以当前观测值 $x_t$ 为输入，以历史窗口 $w_t$ 为时间上下文进行条件约束。
    *   **异常检测机制**：计算数据点的负对数似然（NLL）。若某片段的 NLL 显著高于预期阈值 $\tau$（基于正常数据均值和标准差设定），则标记为异常。
    *   **迭代插补流程**：采用自回归链式生成。从潜在空间的中心（最可能点）采样，通过逆向流函数 $F^{-1}$ 生成替代值，并不断更新时间上下文以生成后续点。
    *   **模型选择与优化**：引入进化算法 **CMA-ES** 进行超参数搜索。定义了综合目标函数，平衡检测性能（AUC-ROC、VUS-ROC）与重建稳定性（自回归预测的均方误差）。
    *   **端到端迭代**：框架在“训练模型”和“改进数据”两个状态间交替，直到数据质量收敛。

### 3. 实验设计
*   **数据集**：
    *   **真实电网数据**：来自挪威配电运营商的电网损耗数据（2017-2023年），包含每小时的耗电量和损耗量。研究者手动标记了受夏令时切换等系统性错误影响的异常段。
    *   **合成基准测试（FSB）**：使用 70 个具有不同基础信号和异常类型的合成序列进行受控验证。
*   **实验场景**：设置了四个不同错误率的训练集（D1-D4，错误率从 0% 到 24.19%）。
*   **对比基准（Benchmarks）**：
    *   **传统插值**：最近邻、线性、二次、三次样条插值等。
    *   **处理策略**：Raw（不处理）、Skip（跳过异常段）。
    *   **深度学习模型**：Dynamix（基于混合专家模型）、KnowImp（基于扩散模型）。

### 4. 资源与算力
*   **硬件环境**：实验在计算集群上运行，使用了从 **NVIDIA P100 到 H100** 的多种型号 GPU。
*   **算力说明**：论文未明确给出每个模型的具体训练时长，但提到使用了 CMA-ES 进行广泛的超参数搜索，这暗示了实验过程涉及大量的模型迭代和计算开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 种不同噪声水平的电网数据集进行了详尽测试。
    *   在 FSB 合成数据集的 70 个序列上进行了验证。
    *   对比了超过 10 种不同的插补方法和 3 种不同的编码器架构（Base, MLP, CNN）。
*   **公平性与客观性**：
    *   为了确保公平，所有对比方法（包括基准插值法）都使用了 CINDI 的模型选择机制来寻找最佳下游检测模型。
    *   使用了 VUS-ROC 等更适合时间序列范围检测的指标，而非仅依赖传统的点检测指标。
    *   实验涵盖了从轻微噪声到严重污染（>20%）的多种场景，评估较为全面。

### 6. 主要结论与发现
*   **CINDI 的有效性**：在错误率低于 13.69% 时，CINDI 能显著提升下游异常检测任务的性能，优于大多数传统插值法。
*   **“跳过”策略的强韧性**：实验发现，简单地在训练中“跳过（Skip）”异常段是一个非常强力的基准，有时甚至优于复杂的插补方法。
*   **噪声水平的限制**：当错误率达到 24.19% 时，所有方法的性能均出现下降。在这种极端情况下，预训练模型（如 Dynamix）表现出更好的稳定性，因为它们不完全依赖于受污染的训练数据。
*   **自回归稳定性**：CINDI 生成的插补数据在统计上与原始序列保持一致，能够维持长达数天的合理预测。

### 7. 优点与亮点
*   **统一性**：将检测与插补集成在单一概率框架内，实现了信息的有效重用。
*   **无监督学习**：不需要大量标注的异常标签即可运行，适合现实中难以获取标签的工业场景。
*   **概率解释性**：基于流模型提供精确的似然估计，能够量化数据点的不确定性。
*   **端到端优化**：通过 CMA-ES 自动优化超参数，降低了人工调参的复杂性。

### 8. 不足与局限
*   **高噪声偏差**：当训练数据中错误比例过高时，模型可能会学习到错误的分布，导致插补结果偏离真实物理情况。
*   **流模型的流形限制**：在完全无噪声的合成数据上，归一化流可能会陷入特定的流形，导致其在检测或插补时失效（需要一定程度的背景噪声引导）。
*   **计算成本**：迭代训练和进化算法搜索超参数的过程计算资源消耗较大。
*   **无法无中生有**：框架能有效去噪，但如果底层物理信号完全丢失或被严重破坏，它无法恢复真实的原始数值，只能生成统计上合理的替代值。

（完）
